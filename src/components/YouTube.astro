---
const { id, title } = Astro.props;
---
<div class="embed" role="group" aria-label={title || 'YouTube video'}>
  <iframe src={`https://www.youtube.com/embed/${id}`} title={title || 'YouTube video'} allowfullscreen allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"/>
</div>
